# Nightly advisory-drift + full validation for aionforge-memory.
#
# Runs on a schedule and on push to `main`. Catches RUSTSEC advisories that
# appear between release gates (the release gate audits at PR time; advisories
# land continuously) and re-runs the full macOS validation off the per-PR path.
# As the substrate moves on `development`, the scheduled run also surfaces
# substrate drift early: it `cargo update`s the selene-db git dependency to the
# latest `development` commit before building, rather than running --locked.
#
# Security: no `${{ github.event.* }}` in any `run:` (confined to if:/with:/env:).

name: nightly

on:
  schedule:
    - cron: "27 8 * * *"
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: nightly-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: short
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"

permissions:
  contents: read

jobs:
  audit-drift:
    name: cargo-audit advisory drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: authenticate to the selene-db substrate
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SELENE_DB_DEPLOY_KEY }}
      - uses: dtolnay/rust-toolchain@1.95.0
      - name: pull the latest substrate commit (drift)
        run: cargo update -p selene-core -p selene-graph -p selene-persist -p selene-gql -p selene-algorithms
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit
      - run: cargo audit --color always

  macos-validation:
    name: macOS full validation
    runs-on: macos-latest
    env:
      CARGO_PROFILE_TEST_DEBUG: 0
    steps:
      - uses: actions/checkout@v4
      - name: authenticate to the selene-db substrate
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SELENE_DB_DEPLOY_KEY }}
      - uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: nextest
      # Nightly builds against the latest substrate commit to surface drift early, so it
      # updates the pinned commit rather than building --locked.
      - name: pull the latest substrate commit (drift)
        run: cargo update -p selene-core -p selene-graph -p selene-persist -p selene-gql -p selene-algorithms
      - name: clippy + tests
        run: |
          cargo clippy --workspace --all-targets --all-features -- -D warnings
          cargo nextest run --workspace --all-features --profile ci
          cargo test --workspace --all-features --doc
